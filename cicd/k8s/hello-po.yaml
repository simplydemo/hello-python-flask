apiVersion: v1
kind: Pod
metadata:
  name: helloflask
  labels:
    app: helloflask
    type: demo
spec:
  containers:
    - image: symplesims/hello-python:latest
      name: helloflask
      ports:
        - containerPort: 8050
          protocol: TCP
